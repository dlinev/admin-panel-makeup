// 'use strict';

let isOnline = true;

const age = 10;
function someName () {
  {
      {
          alert(age)
      }
  }
}

const userName = prompt('Ввведите имя');
const userEmail = prompt('Ввведите почту');
const userPassword = prompt('Ввведите пароль');

alert(`
    name: ${userName}, email: ${userEmail}, password: ${userPassword}
`);


if (age === 20) {
  alert(20)
} else if (age = 21) {
  aler(20)
} else if (age = 20) {
  aler(22)
} else if (age = 22) {
  aler(23)
} else if (age = 23) {
  aler(24)
} else if (age = 24) {
  aler(25)
}

switch(age) {
  case 20: { alert(20);  break;}
  case 20: { alert(20);  break;}
  case 22: { alert(20);  break;}
  case 23: { alert(20);  break;}
  case 24: { alert(20);  break;}
  case 25: { alert(20);  break;}
}
