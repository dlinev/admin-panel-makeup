{
  "name": "lesson-2",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  
  "scripts": {
    "prettier": "prettier --write src/**/*.{js,css,html}"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run prettier"
    }
  },
  "lint-staged": {
    "linters": {
      "src/**/*.{js,css,html}": [
        "prettier --write",
        "git add"
      ]
    }
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "prettier": "2.5.1"
  }
}
